# B36 顶部导航原生拖动保护

2026-09-19 01:31（北京时间）发布至 https://wotbqydailian.vip，运行提交 `e94874fe7441622efbb7887027ac1ec96d6de559`，服务器验收时间 `2026-09-18T17:31:02.963Z`。部署基线为 `887535eb637635c3e5aa97df790496591bb005eb`。

仅为顶部五个 `.site-nav-link` 添加 `draggable="false"` 与阻止默认 `dragstart` 的监听器，脚本引用版本更新以避开旧缓存。链接地址、点击处理、修饰键、键盘导航、配色与布局保持原状，移动端导航、首页标志链接和其他按钮不增加拖动拦截。线上只更新 `public/index.html`、`public/script.js`。

用户确认只有顶部导航拖动会触发故障。故障时脚本仍可执行，页面中心命中正常下单卡片，窗口事件记录没有输出。此版本是阻止可疑原生拖动流程的防护措施；隔离 Edge 环境未复现用户原有卡死，仍需用户在原普通窗口复测，不能将自动验收通过等同于确认卡死根因或故障已消失。

本地与线上分别在 Edge 深浅主题中验证五个导航的真实鼠标拖动均不启动原生拖拽，逐项点击和 Enter 切换正常，Tab 焦点顺序正常。隔离测试中主动恢复单个链接的可拖动属性后，真实拖动仍被限定监听器取消，未产生 `pointercancel` 或 `dragend`。另验证标志链接仍可拖动、主题按钮原有属性与切换正常、390px 移动导航正常，无页面脚本错误。所有业务 API 均使用合成数据，线上仅读取同源静态资源，未写业务记录。本地现有社区页面验收覆盖深浅主题及1440/768/390/360px共8组，均通过。

部署工具核对生产基线、备份并原子发布，失败自动回退。两份运行文件的磁盘与公网规范化SHA256分别为 `8d25990633b17ae56c9c6f57cfe8cfd93eaaea83ba6574d0f618596597903ccb`（index）、`d646cae8c0c7f14ed6cf7336c4bf046760a24bc20edbe57fd816bd48ebcf65fc`（script）。健康接口200，匿名管理员订单接口401；后端没有重启或迁移，PM2 PID仍为311770，环境文件及服务器Git状态未变化。

独立发布目录为 `/root/b36-nav-drag-release-e94874fe74`，权限0700。发布包SHA256为 `4594463eb360c6204930e5a8089370150498c12e08d6341c068ff73e9f778b98`。备份权限0600，`backup/public-before.tar.gz`为7,906,396字节，`backup/runtime-before.tar`为266,240字节。若用户要求回退，先核对线上仍是上述B36哈希，再从 `runtime-before.tar` 单独恢复两份运行文件并核对清单中的基线哈希，保留其他文件；源码以 `git revert e94874fe7441622efbb7887027ac1ec96d6de559` 记录回退并同步云端，避免重置历史。存在后续改动时应撤销本次具体变更，不直接覆盖新版本。

发布与验证工具：`backend/scripts/prepare-b36-release.cjs`、`backend/scripts/deploy-b36-release.cjs`、`backend/scripts/verify-nav-drag-browser.cjs`。
