# B34 主题切换账号保存修复

站点：https://wotbqydailian.vip

2026-09-19 00:46（北京时间）完成部署。运行提交 `06a47e3520e421a4a1e8586dd0686003d4791d45`，服务器验收时间 `2026-09-18T16:46:23.880Z`。

原有顶部主题按钮只更新本地 `theme`，没有写入账号设置。刷新时启动代码读取服务器主题，无条件覆盖本地选择，因此浅色又被账号中旧的深色设置替换。

顶部按钮及设置页主题选项现在立即应用并自动同步账号，保留原有“保存主题”按钮用于主动保存或失败重试。主题同步由 `public/theme-preference.js` 统一处理：保存之前读取设置，确保现有后端GET已初始化账号设置行；多个切换按顺序提交并保留最后选择；旧设置响应不能覆盖更新的选择。同步失败时保留当前主题和按账号保存的待同步标记，刷新仍保持该主题，并在刷新、网络恢复或主动重试时继续同步。登录或账号变化时读取对应账号偏好，旧账号响应不会影响当前账号；访客只保留浏览器主题。未改配色、组件布局、其他业务或后端接口，无需迁移及重启。

新增7项主题状态测试，272项完整测试全部通过。本地浏览器覆盖1440与390像素宽度，检查顶部切换、设置页不点保存也自动同步、刷新保留、同步失败后刷新及重试、无账号缓存时读取服务器偏好、账号切换、延迟读取、连续切换写入串行化及访客刷新保存。生产使用同一脚本验收，账号设置API全部通过模拟数据拦截，静态资源仅GET/HEAD；不操作真实账号数据。

发布工具 `backend/scripts/deploy-b34-release.cjs` 仅发布三个前端文件，模块先于页面发布，缓存版本 `20260919-b34`。逐文件核对生产基线、服务器文件与公网规范化SHA256，全部HTTP200；健康接口HTTP200，匿名管理员订单接口HTTP401。PM2 PID保持311770，环境文件及服务器Git状态未变化。

| 文件 | 规范化 SHA256 |
| --- | --- |
| `public/index.html` | `f70e92f09514d36cca4054d8ac7dfaca3309e557d9c02d1a134ba89d1a47b3e1` |
| `public/script.js` | `4c66eb55a28babc8d837e077e56342ffb2d8c75a9a78776726872ec05a984964` |
| `public/theme-preference.js` | `6660f325dfb146ab7323220b40cebacab67ab094e6f2c7c673a812cb896267a3` |

私有发布目录 `/root/b34-theme-persistence-release-06a47e3520`（0700），发布包SHA256 `4199e53766a7832116ccf519d898948439bf1a811fd2073e2d2ae941f70a4dd5`。备份权限0600；`backup/public-before.tar.gz`为7,905,239字节，`backup/runtime-before.tar`为276,480字节。发布验证失败自动回滚，本次服务器校验成功。

源码、测试、验收与发布工具及报告使用GitHub `kane20060225-cloud/qydailian`，原子快进同步至 `main` 和 `codex/b7-ux-foundation`。报告提交不改变线上运行文件。
