# B17 UI 正式发布记录

站点：https://wotbqydailian.vip

2026-09-16 23:30（北京时间）完成部署及公网校验。运行源码提交：`85a2cf81fda9c356e61a130da35039f2a512bac2`。后续报告提交只补充文档。

## 发布范围

仅替换 `public/` 下 `index.html`、`style.css`、`script.js`、`order-center.js`、`booster-availability.css`、`ui-foundation.css`、`light-theme.css` 七个前端文件。浅色主题独立布局和组件体系、共用界面细节、文案统一、顶部主题切换及主题保存反馈正式上线，缓存版本 `20260916-b17`。

没有数据库迁移、订单或余额修改；生产环境文件哈希不变。后端未重启，PM2 `my-backend` PID 保持 `304289`，健康接口 HTTP 200。服务器依赖、上传文件和 Git 元数据保持原样。

## 备份及回滚

- 发布目录：`/root/b17-ui-release-85a2cf81fd`，权限 `0700`。
- 源码归档 SHA256：`0b3b1df655c9b27b532a5d297521b964d4a56c61e5ed7136469ad41a06ac8050`。
- 发布前核对七个文件的生产基线；新增样式文件确认原先不存在。
- `backup/public-before.tar.gz`：3,715,206 字节；`backup/runtime-before.tar`：399,360 字节。两个备份均为 `0600`，在服务器受保护目录保留。
- `before.json` 和 `verified.json` 权限 `0600`，记录环境哈希、文件清单、进程和公网校验结果。
- 回滚使用本批运行文件备份恢复原有五个文件、移除本批新增的两个样式文件。无需数据库恢复或后端重启；保留本批备份。

归档及公网源码校验将 CRLF 规范为 LF，避免 Windows 文本换行造成误判。归档二进制 SHA256 另行核对。发布校验最初在替换前因换行差异中止，修正后完成；当时没有改动线上运行文件。

## 验收

- 七个公网文件均 HTTP 200，规范换行后的 SHA256 与源码清单一致。
- 未登录访问管理员订单中心 HTTP 401。
- 线上无登录真实浏览器：1440×1000、390×844 验证浅色无背景图、导航及下单区无横向溢出、深浅主题切换及重新加载后保留主题，未发生页面脚本错误。仅允许本站 GET / HEAD 请求，拦截所有写请求及外部域名。
- 上线前本地 220 项测试通过；深浅主题各五种尺寸、下单、订单、删除审核和通知浏览器回归通过，详见重构说明。
- 线上首页效果图保存到本地 `artifacts/ui-preview/live-light-1440-home.png` 和 `live-light-390-home.png`，不纳入运行发布文件。

云仓库同步目标为 `origin/main` 和 `origin/codex/b7-ux-foundation`，通过原子快进推送保留远端历史。
