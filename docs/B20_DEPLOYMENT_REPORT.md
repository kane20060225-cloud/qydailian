# B19 / B20 联合发布记录

站点：https://wotbqydailian.vip

2026-09-17 01:33（北京时间）完成前后端联合部署，运行源码提交 `6c8061592f8397e9887f156ca33a1fd6363cee23`。B19 深色橙色强调恢复、网页代练项目编辑与管理员活动发布，以及 B20 经营指标独立入口、工作状态移入上下线设置和登录设备合并一同上线。

发布18个运行文件，包括12个公开页面/脚本/样式文件；公网全部HTTP 200，规范换行后的SHA256与源码一致。B19配置迁移和B20登录设备增量迁移已执行，两者重复执行成功；迁移仅增加配置表、设备标识列及唯一索引，保留历史记录。原有七个项目以旧内容及旧价格初始化，活动初始为空，等待管理员发布。

发布前在隔离MySQL数据库验证两批实际迁移、重复执行、历史设备保留、同一设备更新浏览器/IP、不同用户隔离和设备列表查询，完成后清理隔离数据库。后端重启后 PM2 `my-backend` PID 为 `305995`，健康接口200，未登录访问登录设备、管理员内容配置及管理员订单中心均401。服务器环境文件哈希及git元数据不变，登录地点接口可从生产服务器访问。

受保护发布目录 `/root/b20-service-workspace-release-6c8061592f` 权限0700；归档SHA256为 `d9665437805eaa588048d0cd1d38522bcb00cca5612642434385fd74051d6360`。备份文件均0600：完整站点运行内容 `backup/site-before.tar.gz` 17,695,676字节（排除git、依赖及验收产物）；本批原有文件 `backup/runtime-before.tar` 542,720字节；数据库 `backup/database-before.sql.gz` 39,650字节。目录还保留发布前基线与发布后验证记录。失败时脚本恢复本批运行文件并重启旧后端；新增数据库结构保留，不覆盖历史业务数据。

235项后端测试通过。两主题各五种宽度、八个页面布局比较通过；项目编辑、动态草稿、活动发布与关联下单在1920/1440/768/390宽度通过；工作状态位置、排班、临时上下线、管理员暂停和未保存排班在1440/1200/390/320宽度通过；登录设备、随机标识持久化、HTML转义、北京时间、读取失败重试和经营指标按需加载在深浅主题1440/390宽度通过。桌面及手机既有下单流程回归通过。上述操作均使用本地合成数据，没有创建或清理生产订单。

公网页面验收仅允许本站GET/HEAD请求，在两主题1440/390宽度检查实际配置加载、首页/下单页布局一致、橙色按钮、无横向溢出和无脚本错误。源码已原子快进推送到 `origin/main` 与 `origin/codex/b7-ux-foundation`，并通过远端引用核对；后续文档提交仅补充发布记录，不替换运行源码。
