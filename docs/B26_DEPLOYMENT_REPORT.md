# B26 打手工作台与收益核对

站点：https://wotbqydailian.vip

2026-09-17 16:21（北京时间）完成最终生产部署。运行提交 `90858c9e4dc355ae9697e184c1084b767bf3f2a8`，工作台资源和主脚本缓存版本 `20260917-b26`。

接单大厅与我的接单由横向表格改为响应式订单卡片，统一现有竞技卡片纹理、配色和按钮。大厅支持项目/订单号检索及客户端筛选，按加急优先、创建时间升序排列，只展示已核实付款且可接订单；接单提交仍按原后端事务检查身份和并发状态。我的接单默认展示进行中，支持已完成/全部筛选。区分预估收益、已入账收益、历史无关联流水预估及测试冲正。完单提供明确确认并禁用正在提交的按钮；原结算规则保持现金结算价75%。新接单通知使用“打手工作台”，不重发或改写历史企业微信消息。

收益页增加近7天、近30天、全部、自选日期（最多366天）筛选。按北京时间日历天和真实流水入账时间统计区间入账、扣减、净收益；累计净收益单独读取账户记录，进行中订单预估不计入收入。明细分页20条，展示订单、项目、现金结算价、入账/扣减金额和真实入账时间。报告通过同连接可重复读事务获得一致快照。历史未关联流水差额单独说明，计入累计记录，不虚构日期或区间收入。

管理员可从正收入明细或测试订单号入口预览冲正。正常订单按原始收益流水精确冲正；历史无原始流水订单必须手动提供核实金额与依据，金额最多两位小数，不超过该单当前75%分成上限。要求订单已完成且已核实付款，预览快照须与订单/账户当前状态一致。事务按订单、打手账户顺序锁定，通过唯一流水和审计键禁止重复扣减；余额不足、状态变化或审计失败不改变收益。冲正写入负收益流水、订单核对说明及操作审计，不删除原订单/流水，不改变客户积分、打手积分或身份。本次发布没有冲正任何真实测试订单，需管理员明确识别后逐单操作。

无数据库迁移，复用 account_ledger、operation_audit 和 order_management_events。发布十个文件（含未变更的 accounting.js 作为隔离检查依赖）。生产前端五个文件 HTTP 200，文本规范换行 SHA256 与运行提交一致。健康接口200，未登录访问财务查询、管理员预览和冲正接口均401。PM2 my-backend 重启后PID310996，环境文件哈希和服务器Git元数据未变。

最终发布目录 `/root/b26-workbench-release-90858c9e4d` 权限0700，归档SHA256 `6b07032cc997146368e768ea59fa589ee7e336f336887ceb92d2a2b4b677c70c`。本轮站点备份18,007,773字节，原运行文件备份522,240字节，备份/清单/记录权限0600。初次改版前完整站点备份在 `/root/b26-workbench-release-1548efee58/backup/site-before.tar.gz`，大小17,995,866字节。发布工具支持恢复已有文件、移除新文件并重启旧后端。

246项测试通过。覆盖北京时间日期边界、用户隔离、原金额精确冲正、重复阻止、过期快照、审计失败回滚、历史金额和依据、非管理员禁止核对。深浅主题在1440/768/390/320宽度浏览器验收大厅筛选、我的接单状态、已入账金额、7/30/自选日期、管理员预览和模拟冲正、角色入口隔离及失败重试，无横向溢出或页面脚本错误。

服务器使用专用连接的临时表和合成数据验证真实MySQL入账报告、冲正事务、重复阻止、失败回滚和历史核对。MySQL临时表不可重复打开，因此收益明细的关联子查询使用临时查找副本；原始生产查询另外在只读事务执行EXPLAIN，四条收益查询及两条订单查询全部通过。临时表在连接关闭后自动消失，无永久业务表写入。

初次生产只读SQL验收发现大厅排序多拼接了旧排序尾部，已修复，并把两条订单查询纳入最终发布前EXPLAIN。最终运行提交通过全部检查。生产浏览器验收使用线上真实前端和浏览器内拦截的合成API，模拟冲正不发往后端，不读取真实用户数据或发送企业微信消息。截图在 artifacts/ui-preview/*workbench-b26-*.png，不上传GitHub。

云同步沿用用户明确授权的GitHub kane20060225-cloud/qydailian 的main和codex/b7-ux-foundation，原子快进推送源码和发布记录，并核对两个分支一致。
