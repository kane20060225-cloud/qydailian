# B24 Velnora 图标发布记录

站点：https://wotbqydailian.vip

2026-09-17 15:01（北京时间）完成生产部署。运行提交 `25bd00c97da0fafee7d0454499a3c85671cfa673`，前端图标与脚本缓存版本 `20260917-b24`。

使用内置 image_gen 工具，从用户确认的效果图中制作独立透明背景图标，保留香槟金几何 V、石墨黑币面和金色外环。生成提示要求：只提取已确认的大圆形 V 币、正面视角、透明背景、无外部投影和文字、保持小尺寸可辨识。使用 sharp 常规缩放生成网页 PNG 和多尺寸 favicon；生成结果未进行额外创意重绘。

生产资源：public/velnora-coin.png（256×256）、public/favicon-16.png、public/favicon-32.png、public/favicon.ico（16/32/48）、public/apple-touch-icon.png（180×180）。浏览器标签页声明 favicon 和 Apple 图标；个人中心积分图、下单积分抵扣标题及商城默认/失败回退图均引用新 Velnora 图标。原 qy-coin.png 留作备份，运行代码不再引用。积分账户字段、余额和兑换规则不变。

仅发布七个前端文件。服务器发布目录 `/root/b24-icons-release-25bd00c97d` 权限 0700，归档 SHA256 为 `968c1696fa80c3e7abe8d6d133692235e63c3e3887e1ca98381c48c06f15362a`。发布前核对既有文件基线与新增文件不存在，先发布图标及脚本再发布首页。公开文件逐项 HTTP 200，文本规范换行后的 SHA256 及图片/ICO 二进制 SHA256 与运行提交一致。

前端完整备份 3,803,030 字节，本批原有文件备份 276,480 字节，备份和验证记录权限 0600。失败时自动恢复原文件并移除本批新增文件。无数据库迁移或后端重启，PM2 my-backend PID 保持 309798，服务器环境文件哈希和 Git 元数据未变。健康接口 200，匿名管理员订单接口 401。

238 项测试通过，本地桌面/手机原业务浏览器检查通过。生产只读浏览器在深浅主题及 1440/390 宽度下通过品牌、积分图实际解码（256 像素）、三个 favicon 声明、favicon/Apple 资源加载、折叠筛选、主题切换及无横向溢出/无脚本错误验收。仅允许本站 GET/HEAD 请求，无业务写入。验收截图位于 artifacts/ui-preview/live-b24-*.png，不同步到云仓库。

云同步目标为用户已明确授权的 GitHub kane20060225-cloud/qydailian 的 main 与 codex/b7-ux-foundation，使用原子快进推送并核对两个分支一致。文档提交补充验收记录，不替换运行提交。
