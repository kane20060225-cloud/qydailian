# B30 下单步骤条排版

2026-09-18 13:52（北京时间）发布至 https://wotbqydailian.vip，运行提交 `c2fde39a08ebdf511bc46da7bbe167a0b42b5a30`。

原步骤条全宽卡片使用靠左flex布局和固定长度连接线，三个步骤挤在左侧，右侧留空。改为等宽步骤网格，各步骤居中，连接线随容器伸缩；桌面上下内边距由18px收紧至12px，各步骤保留44px点击高度。手机布局容纳完整文字和编号，继续支持点击切换及进度状态。

本地和正式站资源在深浅主题 × 320/390/430/600/650/760/1440px下验证：三个步骤间距一致、点击区域高度至少44px、内容不超出卡片；既有客服/下单栏、导航高度变化、筛选触摸滑动和订单纵向滚动回归通过，浏览器无脚本异常。业务接口隔离，不写入生产业务数据；手机验收使用Chromium尺寸与触摸模拟。

沿用B29前端发布工具及四文件清单，其中实际变更为 `index.html` 和 `theme-components.css`，其他两文件内容保持一致。线上逐文件HTTP200及SHA256一致，健康检查200，匿名管理员接口401。无后端重启、无数据库迁移，环境配置保持不变。

私有发布及备份目录 `/root/b29-mobile-release-c2fde39a08`（0700）：`backup/public-before.tar.gz` 为3,973,863字节，`backup/runtime-before.tar` 为317,440字节，备份文件0600。发布包SHA256为 `0b2d2a4cb6b7db07ad4563ab1f79833fd69f4fef1563d0d44604f51dcb0c72f3`；失败时工具自动回滚。

源码、回归验证和本报告同步至既有GitHub仓库 `kane20060225-cloud/qydailian` 的 `main`、`codex/b7-ux-foundation`。
