# B30 订单号浏览器自动填充修复

站点：https://wotbqydailian.vip

2026-09-18 14:35（北京时间）发布运行提交 `355730ce0148a6038edb26135a44ba21e74b1f7e`，服务器验收时间 `2026-09-18T06:35:58.881Z`。

## 修复

- 三方订单“核实收款”的交易单号输入框增加 `autocomplete="off"`，关闭浏览器表单历史自动填充，并关闭拼写检查。
- 新建三方订单和修改重新提交的外部订单号输入框增加同样的自动填充设置。
- 主脚本资源版本更新为 `20260918-order-autofill`，页面刷新后请求新版脚本。
- 已保存在浏览器中的表单历史不能由网站删除。Edge 用户可按 Ctrl+Shift+Delete，时间范围选所有时间，仅勾选自动填充表单数据后清除；这也会清除其他网站的表单填写记录，不影响服务器订单与收款记录。

## 验证与发布

- 265 项现有自动测试全部通过，主脚本和新增发布工具语法检查、差异空白检查通过。
- 发布前逐文件校验生产基线，发布后逐文件校验磁盘和公开 HTTP 内容的规范化 SHA256，两个文件均 HTTP200 且与运行提交一致。
- 健康接口 HTTP200，管理员订单接口匿名访问 HTTP401。
- 仅发布 `public/script.js` 和 `public/index.html`，资源先于页面发布；没有数据库迁移或后端重启。PM2 PID 保持311770，环境配置和服务器 Git 版本保持不变。
- 自动填充属性已在发布内容中验证；未操作用户浏览器中的历史记录，也未进行该用户 Edge 配置下的历史下拉弹窗实测。

## 备份

私有发布目录 `/root/b30-order-autofill-release-355730ce01`（0700）。备份文件权限0600：

| 文件 | 字节 |
| --- | ---: |
| `backup/public-before.tar.gz` | 3,973,910 |
| `backup/runtime-before.tar` | 276,480 |

发布包 SHA256：`64b98afdd00e5685b6594d9413d118165e17819d76fed5d5063bf36e727f19d8`。

发布工具在验收失败时自动恢复原文件；本次验收成功。服务器保存 `before.json` 与 `verified.json`。

## 云同步

源码、带备份及回滚的 B30 发布工具和本报告同步至现有仓库 `kane20060225-cloud/qydailian` 的 `main`、`codex/b7-ux-foundation`。线上运行文件以本报告开头的运行提交和发布哈希为准。
