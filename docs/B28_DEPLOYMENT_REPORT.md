# B28 人工客服与搜索式权限管理发布

站点：https://wotbqydailian.vip

2026-09-17 17:25（北京时间）完成发布，运行提交 `b825d4e14e3bb99aa1a49ca54c99256ff234dd21`。

## 上线内容

- 全站“联系客服”，自己的订单详情关联订单咨询；文字、私有截图、历史消息、未读数、断网补齐和发送去重。
- 客服工作台集中处理待回复咨询，按等待时间优先排列；旁边显示安全订单摘要，提供快捷回复和处理状态。
- 新增客服角色；工作台仅对客服和管理员开放，普通用户、打手不能访问客服侧。
- 权限管理默认搜索用户名或用户 ID，支持角色筛选，保留“浏览全部用户”的分页入口；权限保存有审计和旧会话撤销。
- 手机聊天全屏，键盘可用高度缩小时输入区保持可见；联系客服按钮避开手机底部导航。

管理员现在可以自行在“管理员后台 → 权限管理”中搜索账号并设为客服。根据本轮用户说明，不预先指定客服账号，既有32个用户角色保持不变（管理员1、打手22、普通用户9）；管理员可立即使用客服工作台。服务时间尚未设置，用户看到可以留言；实际时间由管理员在工作台设置。

## 迁移、备份与恢复

新增 `support_settings`、`support_conversations`、`support_messages`、`support_staff_reads` 四张表。没有修改旧订单、余额或既有角色。生产环境文件及服务器 Git 元数据保持不变，PM2 `my-backend` 重启后验收 PID `311770`。

私有发布目录 `/root/b28-support-release-b825d4e14e` 权限0700。归档 SHA256：`bb45fb9c9e4d69af0715c0bc883eea64b6ecf5dcc83d1144bbfe897baa974df5`。

| 私有备份 | 大小 |
| --- | ---: |
| `backup/site-before.tar.gz` | 18,017,282 字节 |
| `backup/runtime-before.tar` | 563,200 字节 |
| `backup/database-before.sql.gz` | 45,009 字节 |

备份权限0600，未上传到 GitHub。发布工具会在运行文件验收失败时恢复原文件、移除新增代码文件并重启旧后端；新增客服表保留，避免删除后续消息。手工恢复须保留 `backend/private-support/` 的私有截图以及客服消息数据。

## 验证

- 本地265项自动测试通过，含13项新增客服、权限和企业微信测试。
- 克隆生产表结构到独立测试库，仅插入合成数据：重复迁移、并发消息重试、截图权限、订单摘要、等待状态、未读数、124条历史消息补齐、用户搜索、权限保存和登录撤销均通过；测试库及测试截图已清理。
- 正式前端8个文件HTTP200，与运行提交内容哈希一致。健康检查200，线上SSE经过Nginx代理返回有效首帧。
- 使用现有管理员账号验证工作台查询和权限搜索；使用普通用户验证客服工作台、提醒诊断和权限管理均403。未登录访问私有截图等接口401。
- 真实线上前端资源配合隔离业务接口，在深浅主题 × 1440/390/320px 下验证聊天、相同消息ID重试、订单摘要、手机输入区域和权限管理；业务写入被测试接口替代，没有新增生产测试会话或变更账号角色。

## 企业微信与手机确认

现有自建应用已开启，管理员账号绑定匹配当前企业和应用，提醒偏好有效。连接验证通过，仅向用户本人的管理员账号发送一条明确标记的“客服上线测试提醒”，没有通知其他成员。

提醒记录36，企业微信发送状态 `sent`，首次发送成功，失败码为空。用户在当前对话中明确回复“已收到”，手机实际接收验收通过。服务器私有记录 `wecom-test.json` 与 `verified.json` 已保存 `confirmed_by_owner`。

后续客服成员需加入企业微信应用可见范围、在工作台绑定自己的账号，并确认手机通知权限。“离站提醒与服务时间”会展示就绪人数和近期发送结果。

## 云同步

沿用既有 GitHub 仓库 `kane20060225-cloud/qydailian` 的 `main`、`codex/b7-ux-foundation`。源码、发布工具与本说明通过原子快进推送同步；服务器运行代码以本报告开头的运行提交及逐文件哈希为准。
