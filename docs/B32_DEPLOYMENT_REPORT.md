# B32 恢复工具与租号横排子菜单

站点：https://wotbqydailian.vip

2026-09-18 23:57（北京时间）完成生产部署。运行提交 `909616c56f03686ca2312a69a4ca2892322d4f62`，服务器验收时间 `2026-09-18T15:57:33.931Z`。

删除 B31 中强制导航占满一行、换到标题下方的样式，以及新增的下划线和按钮形态覆盖。实用工具与账号租赁恢复原有标题右侧的横排子菜单；窄屏沿用原有换行及横向滚动规则。原配色、卡片优化和场均计算器修复保留。

样式缓存版本 `20260918-b32-inline-nav`。仅发布 `public/community-pages.css` 与 `public/index.html`，样式先于页面发布；逐文件核对生产基线、磁盘及公网规范化 SHA256，两个文件均 HTTP200。健康接口 HTTP200，管理员订单匿名接口 HTTP401。没有数据库迁移或后端重启，PM2 PID保持311770。

本地及生产前端浏览器验收覆盖深浅主题、1440/768/390/360像素宽度。新增断言确保桌面工具与租号的全部子菜单按钮位于标题右侧，并与标题垂直居中。原有无横向溢出、配色继承、子页切换及计算器检查继续执行。生产仅允许同源GET/HEAD，API使用合成数据，不写入业务数据。

| 文件 | 规范化 SHA256 |
| --- | --- |
| `public/index.html` | `63eccf212eb0aed550fb01f21a5f1164c822101672534866fa71555597e33542` |
| `public/community-pages.css` | `5dda937770a2d19265431c99dfa385fe45eaed958111fbd31b9553fb2d77c40f` |

私有发布目录 `/root/b32-inline-nav-release-909616c56f`（0700）。发布包 SHA256 `aea6bcb7a095c3f95e98e0fdfb7b857fd9c8c518fa51f3a6df038fd7605858c8`。备份文件权限0600，`backup/public-before.tar.gz`为3,974,813字节，`backup/runtime-before.tar`为81,920字节。发布工具验证失败时自动回滚；本次服务器验证成功。

源码、发布工具、验收脚本及报告沿用 GitHub `kane20060225-cloud/qydailian`，原子快进同步至 `main` 和 `codex/b7-ux-foundation`。报告提交不改变线上运行文件。
