# B31 四个板块形态统一与场均计算器修复

站点：https://wotbqydailian.vip

2026-09-18 23:49（北京时间）完成生产部署。运行源码提交 `f8d619c31f8558da95994c44a776f3e638839881`，服务器验收时间 `2026-09-18T15:49:58.190Z`。

## 修改

- 保留原本主题配色、背景图和渐变面板；深色沿用橙色强调，浅色沿用蓝色强调。四个板块不覆盖原主题颜色变量。
- 统一租号、公告、新闻和实用工具的宽度、标题、导航、圆角和留白。去掉这四页卡片的战术网格装饰，使用导航下划线和资讯分隔布局，与下单页的项目选择和流程卡片区分。
- 修复场均计算器字段标签及输入框布局，改用伤害单位、1500/2000/2500 示例和场均说明，移除胜率模式的 100 上限。切换模式清空指标输入及旧结果，保留总场次。
- 拒绝负数、非法胜率及非整数场次；当前指标已达目标时直接显示达标结果。
- 样式缓存版本 `20260918-b31-shape2`，主脚本版本 `20260918-b31-calculator`。

## 验证与发布

- 本地及生产前端浏览器检查均通过：深浅主题，1440、768、390、360 像素宽度。覆盖四个板块、租号子页、工具切换、原配色继承、无横向溢出、无脚本错误、场均与胜率计算及输入校验。
- 生产浏览器只允许同源 GET/HEAD，所有 API 使用浏览器内合成数据，不登录真实账户，不读取真实用户数据，不写入业务数据。截图在 `artifacts/ui-preview/live-community-ui/`，不同步到云仓库。
- 发布前规范换行并校验生产基线；发布后校验磁盘及公开 HTTP 文件 SHA256，三个文件均 HTTP200 且与运行提交一致。
- 健康接口 HTTP200，管理员订单接口匿名访问 HTTP401。
- 仅发布三个前端文件，样式和脚本先于页面发布。没有数据库迁移、后端重启或环境配置修改；PM2 PID 保持311770。

| 文件 | 规范化 SHA256 |
| --- | --- |
| `public/index.html` | `d74751246b7e07e844a255cb67e4575daa970b6faf761972dc7fea39b423f5e6` |
| `public/script.js` | `222404d3995569424fb39475c2b2c78e11b11052515e26cfaa0a29da13752a63` |
| `public/community-pages.css` | `e7fa5291faf4a6bd14d1ac200d84d4e5118839b5b75108dcbfe4519ab09970f2` |

## 备份

私有发布目录 `/root/b31-community-ui-release-f8d619c31f`（0700）。发布包 SHA256 `86be76cba965a75c7dc36adb3a86c93649c17332676085f59a31f69a5be383cf`。

| 备份文件（0600） | 字节 |
| --- | ---: |
| `backup/public-before.tar.gz` | 3,973,951 |
| `backup/runtime-before.tar` | 276,480 |

发布工具在验收失败时自动恢复原文件并移除新增样式；本次验收成功。服务器保存 `before.json` 与 `verified.json`。

## 云同步

源码、发布工具、浏览器验收脚本及本报告沿用 GitHub 仓库 `kane20060225-cloud/qydailian`，原子快进同步至 `main` 和 `codex/b7-ux-foundation`。后续验收脚本与报告提交不改变上述线上运行文件。
