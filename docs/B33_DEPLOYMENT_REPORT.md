# B33 军需库与坦克卡片抽选

站点：https://wotbqydailian.vip

2026-09-19 00:30（北京时间）完成生产部署。运行提交 `382c388638b54a1fea1e6bb20cf40bdffb117c20`，服务器验收时间 `2026-09-18T16:30:30.638Z`。

本次只调整开箱模拟器、“这把玩什么”及其开箱结果、仓库弹窗。独立样式 `game-tools.css` 使用原有主题变量，保留深浅主题配色、页面背景和标题右侧子菜单。其他板块没有形态或功能改动。

开箱模拟器改为军需库：余额与签到、仓库入口集中在顶部，充值为次要入口；桌面采用集装箱目录与已选奖池并排布局，手机纵向排列，点击集装箱自动定位详情并提供返回目录按钮。统一箱子图片尺寸、名称、说明和价格；奖池直接读取现有后台配置，并说明稀有物品单次获得概率、普通奖池条件概率及首项保底。开启期间防止重复请求，结果以奖励卡片展示，揭晓动画可以跳过。仓库突出物品、稀有度与数量，获取时间和来源收在详情内，加载失败可重试。后端开箱、扣券、签到和充值规则保持原状。

“这把玩什么”采用第二种卡片滚动方案，使用真实坦克图片。支持等级、车型和国家筛选；展示符合条件的候选数量、抽中坦克的图片、名称、等级、车型与系别。支持重抽、排除当前坦克后再抽、恢复排除及重置条件。动画跳过或窗口改变时保留已确定的结果；候选池为空时给出提示。桌面结果在1440×960首屏可见，手机适配单列布局。

坦克资料取自 [BlitzKit 项目](https://github.com/blitzkit/blitzkit) 与其公开定义和图片接口，2026-09-19快照包含718辆非测试、非弃用的I–X级坦克。图片718张均在站内托管，合计3,903,870字节，浏览器无需访问外部图片接口；图片失败提供本地占位图。坦克名称沿用源数据，等级、车型与系别标签为中文。目录为版本快照，不在每次抽选时实时更新。

本地265项现有测试全部通过。`backend/scripts/verify-game-tools-browser.js` 本地及生产验收均覆盖深浅主题与1440/768/390/360像素宽度，共8组。检查奖池选择、防重复开箱、服务端余额、奖励和仓库卡片、加载失败重试、登录限制、筛选、重抽、排除池耗尽及恢复、实际动画跳过和窗口变化；检查无浏览器异常、无横向溢出及桌面结果首屏位置。另对主页、下单、租号、公告、新闻和计算器逐个比较旧版与新版的可见元素位置、尺寸、字体、间距和配色，全部一致。生产所有API请求使用合成数据拦截，只允许静态资源GET/HEAD，不写入真实业务数据。

缓存版本 `20260919-b33`。发布724个前端文件：页面、主脚本、两个工具的独立样式和脚本、坦克目录、本地占位图及718张WebP图片。所有资源先于页面发布；逐文件核对生产基线、磁盘及公网SHA256，全部HTTP200。健康接口HTTP200，管理员订单匿名接口HTTP401。无需数据库迁移或后端重启，PM2 PID保持311770，环境文件及服务器Git状态未变化。

| 文件 | 规范化 SHA256 |
| --- | --- |
| `public/index.html` | `92eda77d942e9c5a8b732ccbc8aa582e715a55cddb8ef8b68a8347efc11eeb15` |
| `public/script.js` | `4d00c213de4fcc9b92cd76431a2ef1c2c9f01d7b66ace05b5ceabb78a738f75b` |
| `public/game-tools.css` | `e8c19584e431853123fc0c89e6a9a3f7a0d45b4253848003d9bb05f4f66b7bf5` |
| `public/game-tools.js` | `8d311174798c606ea55db73cc4a0450f0b1ba3fa04bdba02742f30545560a213` |
| `public/tank-catalog.js` | `dfedfcf3ed3da8a2202840f21cfc0eeb2125a0a5c6490486f717a11d4560528d` |
| `public/images/tank-silhouette.svg` | `60df9fae7c55e6ef213aad40081ab1751f7211d680810f9c5aaa53e6dfd45751` |

私有发布目录 `/root/b33-game-tools-release-382c388638`（0700）。发布包SHA256 `2a8acfe621327079c2ce2920051671a8acbb528188c25deca3e3efb7ded3fa88`。备份权限0600；`backup/public-before.tar.gz`为3,975,371字节，`backup/runtime-before.tar`为276,480字节。发布工具 `backend/scripts/deploy-b33-release.cjs` 限定文件清单，发布验证失败时自动恢复旧文件并移除新增文件；本次服务器校验成功。

源码、坦克资源、发布及验收工具和报告使用GitHub `kane20060225-cloud/qydailian`，原子快进同步至 `main` 和 `codex/b7-ux-foundation`。报告提交不改变线上运行文件。
