# B14 正式部署记录（2026-09-16）

- 站点：`https://wotbqydailian.vip`，服务器 `59.110.150.192`。
- 运行路径：`/var/www/your-site`；PM2 服务 `my-backend`。
- 部署运行代码提交：`d5982fa73e41fd94cdd4cbed4b2cbe6baf21de4e`。
- GitHub：`kane20060225-cloud/qydailian`，`main` 与 `codex/b7-ux-foundation` 均已原子快进同步 B14。
- 服务器发布目录：`/root/b14-order-lifecycle-release-cSLzPtT1`。
- 发布包 SHA256：`f007c3e873629d25fb2caff48e6d0d20fb8a3511e0ae48daedfdcd80c96f928a`。
- 站点备份：发布目录下 `backup/site-before.tar.gz`，权限 `600`。
- 数据库备份：发布目录下 `backup/database.sql`，权限 `600`。
- 运行文件回滚包：发布目录下 `backup/runtime-before.tar`。

生产 B14 迁移已应用并复验，只新增 `recharge_order_resolutions`、`order_lifecycle_settings` 和迁移版本记录。迁移后两张新表均为空；没有自动核销、取消或补发历史订单。24小时超时关闭默认未启用。

服务器 Node.js `v22.23.1`、MySQL `8.0.45`；服务器自动测试 `202/202` 通过。专用空测试库使用生产表结构副本、合成用户及模拟签名查询结果，验证真实 MySQL 下的并发退款／补发、退款后软删除、历史已到账不改余额不伪造流水、超过200条受保护记录不阻塞超时筛选、历史租单缺流水保护、事务失败回滚、重复通知不重复发券、后到支付成功使测试核销和回收站快照失效。专用测试库仅含合成记录，上线后清理。

14个运行文件按白名单原子替换，逐项校验内容；重启服务后 PM2 在线，PID `303041`。公网 HTML、主脚本、订单中心脚本及样式均返回200且与部署提交内容哈希一致。健康接口正常；匿名订单中心、超时预览及充值详情保持401；使用既有管理员的短时内存会话完成订单中心、超时预览和异常充值详情只读验证，会话不写入文件或日志。生产 `.env` 内容哈希前后一致。

桌面 `1440×1000`、手机 `390×844` 的本地合成浏览器验证通过，包含原订单中心筛选、删除／恢复、支付入口及新取消、核销、超时预览确认流程，无页面脚本错误或横向溢出。

本次没有进行真实支付宝付款、退款或补发，也没有对真实订单做测试核销。后续按 [B14_ORDER_LIFECYCLE_RUNBOOK.md](B14_ORDER_LIFECYCLE_RUNBOOK.md) 从管理员订单详情逐笔核对实际付款与发券记录后操作。
