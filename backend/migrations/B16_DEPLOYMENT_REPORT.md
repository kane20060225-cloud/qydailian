# B15 / B16 正式发布记录

发布站点：https://wotbqydailian.vip

线上只读验收完成：2026-09-16 22:56（北京时间）。运行源码提交：`7128069e83a0d1db6878482f707e132d77e2c43e`。本报告后续提交仅补充文档。

## 已上线内容

B15 三步下单、安全选项草稿、订单下一步指引、租号筛选与价格比较、管理员等待优先级及经营指标，以及 B16 三方订单删除申请、管理员审核、历史异常代练核对删除。

打手只能为自己的三方订单申请删除，提交原因后由管理员审核。同意或直接核对删除需填写原因、核对依据并确认保留记录。删除进入可恢复回收站，核对删除的原订单、交付、资金及操作记录长期保留，不自动永久清理。原支付状态和余额不因删除而改变。

已针对用户提供的 `WOT1789399580499K08Y8Q` 执行管理员只读详情验收：仍为历史状态异常，返回 `reviewed_remove` 操作，不再返回上传付款凭证操作。此订单未被自动删除。生产新增审核表在验收时为零条，发布没有提交任何真实订单删除申请、审核或退款。

## 发布与备份

- 服务器发布目录：`/root/b16-order-deletion-release-2effee91c7`，站点目录：`/var/www/your-site`。
- Git 源码归档 SHA256：`508affd4860e5efe98475e908915c6f7da5a73f4d78a1812a342ff71771d217a`。
- 白名单替换 32 个已审核文件；保留生产 `.env`、上传文件、依赖和服务器 Git 元数据。环境文件哈希一致。
- 唯一新增表 `order_deletion_requests` 的结构及迁移版本已验证。B14 自动超时设置保持原值，不改写历史订单、收款、流水或余额。
- PM2 `my-backend` 重启后在线，验收 PID `304289`，健康接口 HTTP 200。
- 受保护备份：`backup/site-before.tar.gz`（20,650,197 字节）、`backup/database.sql`（208,401 字节）、`backup/runtime-before.tar`（542,720 字节），均为权限 `0600`。备份含生产敏感信息，只保留在服务器受保护目录。
- 运行代码回滚使用本批运行文件备份并重启 PM2；新增审核表保留，以免丢失审核记录。全量站点和数据库备份用于必要时恢复，不自动覆盖后续业务数据。

## 验证证据

- 本地及服务器后端测试：220 / 220 通过。
- 真实浏览器桌面 1440×1000、手机 390×844：三步下单、草稿、租号比较、订单指引、删除申请、审核确认、记录保留及恢复通过；原订单中心、B14 生命周期、通知、打手状态回归通过。
- 服务器独立 MySQL 8.0 表结构副本，合成数据验证并发申请、同意/驳回、审计失败回滚、长期记录保留、状态变化重新可见与经营指标 SQL。专用测试库及账号验证后已删除。
- 公网 HTML、脚本和样式六项 SHA256 与发布清单一致。
- 未登录访问订单中心、订单详情和审核接口均返回 401。
- 管理员只读验证订单列表、经营指标、超时预览、三方订单列表及图片订单详情全部 HTTP 200。验收会话为内存中两分钟令牌，未写入文件或输出。
- 服务器 `prepared.json`、`deployed.json`、`verified.json` 保存执行证据，权限 `0600`。

云仓库同步目标：`origin/main` 与 `origin/codex/b7-ux-foundation`，使用原子快进推送，不强制覆盖远端历史。
