# 青衣代练工具站：项目分析

> 分析日期：2026-09-14
> 审阅范围：仓库全部源码、配置、前端静态资源索引与 Git 历史；未修改既有代码。
> 当前工作区：Git 状态干净。本文件是本次分析新增的唯一文件。

## 1. 项目定位与技术栈

这是一个面向《坦克世界闪击战》用户的代练/工具站原型。它同时提供常规代练下单、打手接单、积分体系、账号租赁、开箱模拟、内容发布与后台运营能力。

| 层级 | 实际技术 | 说明 |
| --- | --- | --- |
| 前端 | 原生 HTML5、CSS3、原生 JavaScript | 没有 React/Vue、打包器、路由库或组件库；单页内以显示/隐藏区块完成导航。 |
| 后端 | Node.js、Express 5（CommonJS） | 一个 `server.js` 承担启动、路由、中间件、数据库初始化和业务逻辑。 |
| 数据库 | MySQL（`mysql2/promise`） | 服务启动时以 `CREATE TABLE IF NOT EXISTS` 与 `ALTER TABLE` 自动建表/补列。 |
| 身份认证 | `bcryptjs` + JWT | 密码哈希；JWT 有效期 7 天，前端以 Bearer token 调 API。 |
| 其他 | `cors`、`dotenv`、支付宝 SDK（代码中引用） | 图片以 Base64 上传到本地文件系统；支付宝 SDK 被引用但当前未安装。 |
| 静态资源 | PNG 背景、二维码、青衣币图及 9 个箱子图片 | 位于根目录 `public/`。 |

后端实际依赖为 `bcryptjs`、`cors`、`dotenv`、`express`、`jsonwebtoken`、`mysql2`。没有测试框架、代码检查、格式化、启动脚本、容器化或 CI 配置。

## 2. 前后端结构

```text
浏览器
  └─ public/index.html
       ├─ public/style.css       主题、响应式布局与模块样式
       └─ public/script.js       页面状态、DOM 渲染、表单和 fetch 调用
              └─ /api/*
                   └─ backend/server.js
                        ├─ Express/JWT/角色中间件
                        ├─ MySQL 连接池与启动建表
                        ├─ 本地 uploads/ 图片目录
                        └─ 支付宝下单与回调
                             └─ MySQL
```

### 前端

- `public/index.html`：单页 HTML，定义主页菜单、业务区块、后台面板与所有弹窗。
- `public/script.js`：约 3,500 行。维护前端价格配置、登录态、区块切换、动态 HTML 渲染、文件转 Base64、以及所有 API 调用。
- `public/style.css`：约 1,500 行。深浅两套主题、桌面/移动端响应式规则和各业务模块样式。
- `API_BASE` 固定为相对路径 `/api`；同源部署时由同一个 Node 服务提供 API。
- 页面不是 SPA 框架路由：点击菜单后由 `showSection()` 切换不同 `content-section` 的显示状态。

前端页面模块包括：

1. 主页：代练、工具、游戏资讯、公告、租号入口。
2. 代练下单：项目/方案/数量/打手档位/客户端/账号信息/加急/积分抵扣与价格展示。
3. 工具：胜率/场均计算器、箱子模拟器、随机坦克转盘。
4. 用户：注册、登录、个人资料、订单、设置、站内邮箱、登录设备。
5. 角色工作台：管理员、打手、三方订单。
6. 运营模块：公告、游戏新闻、积分商城、开箱配置、租号。

### 后端

- `backend/server.js` 是单体服务（约 2,200 行），没有 controller/service/model 分层。
- 中间件：静态文件、CORS、JSON/URL 编码解析、JWT 鉴权、管理员鉴权、打手鉴权、注册 IP 次数限制。
- 角色为 `user`、`booster`、`admin`；打手另有 `gold`、`silver`、`standard`、`budget` 身份等级。
- 图片上传不使用对象存储或 multipart；请求中的 Base64 内容直接写入 `backend/uploads/`，随后通过 `/uploads` 暴露。
- 路由数量较多，按领域可归为：

| 路由领域 | 已有 API 能力 |
| --- | --- |
| 认证与用户 | 注册、登录、资料、积分、设置、账号信息变更、消息、设备记录。 |
| 常规订单 | 创建订单、支付凭证上传、订单详情/列表、后台审核/改状态/上大厅、打手大厅/接单/完单/收益。 |
| 管理员 | 用户角色、打手身份、定制需求、内容、商城、箱子。 |
| 积分与开箱 | 积分商品浏览/购买，箱子配置、签到领券、开箱、仓库、支付宝充值和异步回调。 |
| 租号 | 账号图片上传、发布/审核/上下架、公开大厅、创建/确认/完成/取消租赁订单、双方订单与收益。 |
| 内容 | 公告、游戏新闻的公开读取与后台 CRUD。 |
| 三方订单 | 打手/管理员创建，权限列表、审核、申请完单、管理员标记支付、删除。 |

## 3. 数据库设计

数据库名默认是 `wotbqydailian`。所有表均为 InnoDB/utf8mb4；外键大多使用级联删除。数据定义目前嵌在 `initDB()` 中，没有独立 migration 文件。

| 领域 | 表 | 主要字段及关系 |
| --- | --- | --- |
| 用户 | `users` | 用户名、密码哈希、联系方式、余额/信誉、推荐关系、角色、游戏账号字段、打手收益/积分/等级、开箱券和签到日期。`referrer_id → users.id`。 |
| 用户附属 | `user_settings` | 一对一用户设置：主题、语言、通知、隐私、默认下单和 2FA 预留字段。 |
| 用户附属 | `user_messages` | 站内信标题、正文、已读状态。`user_id → users.id`。 |
| 用户附属 | `login_devices` | 设备信息、IP、登录时间。`user_id → users.id`。 |
| 代练 | `orders` | 订单号、用户、项目/方案/数量/价格、状态、支付状态/凭证、打手、游戏账号信息、客户端、所需打手身份。`user_id → users.id`；`booster_id` 目前没有外键。 |
| 定制 | `custom_requests` | 用户的需求、联系方式、预算、可用时间、状态。`user_id → users.id`。 |
| 积分商城 | `qy_shop_items` | 商品、说明、图片、积分价格、库存、上架状态。 |
| 积分商城 | `qy_purchases` | 用户购买流水，记录商品快照价格与名称。关联用户和商品 ID。 |
| 开箱 | `chest_configs` | 箱子名称、图片、价格、描述。 |
| 开箱 | `chest_items` | 稀有物品与权重。`chest_id → chest_configs.id`。 |
| 开箱 | `chest_common_rewards` | 普通奖励、数量范围、掉率。`chest_id → chest_configs.id`。 |
| 开箱 | `user_inventory` | 用户库存；用户/物品/箱子/稀有度唯一，保存累计数量。 |
| 开箱 | `user_chest_records` | 每次开箱掉落记录。 |
| 支付 | `payment_orders` | 支付宝商户订单号、用户、金额、`pending/paid/closed` 状态。 |
| 内容 | `announcements`、`game_news` | 标题、正文；均带创建/更新时间。 |
| 租号 | `rental_accounts` | 发布者、游戏 UID、客户端、坦克、时价/日价、截图 JSON、规则、审核/上架状态。 |
| 租号 | `rental_orders` | 租用人、发布者、账号、租用单位和数量、金额/积分、状态。关联用户与租号账号。 |
| 三方订单 | `third_party_orders` | 创建者、平台、代练内容、账号信息、金额、审核/完单申请/支付状态、审核人。 |

主要业务关系可概括为：`users` 是中心实体；一个用户可创建常规订单、定制需求、租号账号/订单、三方订单、购买记录、库存、消息和设备记录。订单、商城购买、租号完单和开箱会分别改变积分、收益或库存。

## 4. 已实现功能

### 已可从代码中确认的完整链路

- 注册与登录：bcrypt 密码校验、JWT 签发、推荐码和推荐积分、注册 IP 日限额、登录设备记录。
- 常规代练：客户创建订单 → 上传支付截图 → 管理员确认收款/上大厅 → 符合身份的打手接单 → 打手完单 → 打手收益/积分、客户积分/VIP、站内信更新。
- 用户账户：资料查看、用户名/密码/电话/邮箱修改，主题、默认下单、通知、隐私等设置的存取，消息已读管理与设备列表。
- 积分体系：注册/推荐/完单获得青衣积分；常规订单和租号订单可用积分抵扣；商城扣积分和扣库存有事务处理。
- 开箱：默认箱子和奖池初始化、每日领券、权重稀有掉落与普通奖励、库存累计、管理员奖池编辑；包含支付宝充值意图。
- 租号：账号提交、图片上传、管理员审核接口、公开展示、租用创建、发布者确认/完成/取消、出租收益统计。
- 内容运营：公告、游戏新闻的公开展示和管理员新增/编辑/删除；内容编辑器支持图片插入。
- 辅助体验：价格计算与一键复制订单、胜率/场均计算器、随机坦克转盘、响应式布局和深浅主题。

## 5. 当前存在的问题

以下按风险和影响排序。结论基于代码审阅以及一次本地启动诊断；未连接实际数据库，也没有执行会写入数据的操作。

### P0：当前无法正常启动/部署

1. **支付 SDK 未安装。** `server.js` 引用了 `alipay-sdk`，但 `backend/package.json` 与锁文件均未声明它。实际启动在该处以 `Cannot find module 'alipay-sdk'` 终止。
2. **支付宝密钥路径硬编码为 Linux 生产机绝对路径。** 启动时无条件读取 `/var/www/your-site/backend/...pem`；即使安装 SDK，在当前 Windows 项目或其他机器上也会因密钥文件不存在而失败。
3. **静态目录指向错误位置。** 服务使用 `path.join(__dirname, 'public')`，实际前端在仓库根 `public/`，而非 `backend/public/`。即使服务能启动，也不会按当前仓库结构提供页面。
4. **缺少标准运行入口。** `package.json` 的 `main` 指向不存在的 `index.js`，仅有一个失败的占位 `test` 脚本，没有 `start`/`dev`。

### P0：安全与资金/账号数据风险

1. **数据库密码和 JWT 默认密钥写在源码中。** 默认数据库口令与可预测 JWT fallback 都必须移出代码并轮换；当前 `.gitignore` 虽忽略 `.env`，仓库没有 `.env.example`。
2. **游戏登录密码以明文保存在 `users`/`orders`。** 这是高风险凭据数据。数据库泄露、后台导出或日志失误都会直接暴露客户账号。
3. **订单金额、数量、打手档位由浏览器传入并被后端信任。** `/api/orders` 直接写入 `price`、`total_price`、`quantity`、`player_type`；后端没有按服务端价格表重新计算，也未限制正数范围。客户端可篡改请求以创建异常低价、负价或不一致订单。租号订单的租用类型/数量同样缺少白名单和正数校验。
4. **上传接口缺少内容型别、大小、图片真实性和访问控制设计。** Base64 内容会直接写为 `.png`；只依赖全局 JSON 10 MB 限制，未校验 MIME/解码结果、未做病毒扫描、配额、删除或对象存储策略。
5. **动态 HTML 存在 XSS 面。** 前端广泛将用户名、备注、租号资料、新闻等插入 `innerHTML`。内容图片渲染器在转义后又恢复标签/引号，不能作为安全净化器。普通用户可控租号文本尤其可能形成存储型 XSS。
6. **令牌存于 localStorage 和可由 JavaScript 读取的 Cookie。** Cookie 没有 `Secure`/`HttpOnly`；一旦发生 XSS，JWT 可被窃取。JWT 也没有刷新、注销失效或服务端撤销机制。

### P1：支付、权限与业务一致性

1. **支付宝仍是未完成的沙箱集成。** 默认 gateway 是沙箱；缺少环境校验、生产/沙箱隔离、金额/应用/卖家等回调字段核验、支付订单过期关闭和对账流程。回调先查 pending 再分别加券/改状态，没有单事务锁定；并发通知可能造成重复加券。
2. **订单状态机未集中定义。** 管理员、打手和租号双方的状态更新分散在路由内；可出现不符合业务预期的迁移，取消没有退款/积分回退，租号没有占用时间段也没有防止同一账号被并行租用。
3. **库存/资金审计不足。** 商城购买虽有事务，但没有库存/积分流水总账；代练积分抵扣、打手收益、租号收益、充值都没有不可变审计表或退款机制。
4. **权限与后台能力不完整。** 后端有租号审核 API，但管理员页面没有对应审核入口；创建管理员的初始引导/种子流程也不存在。``two_factor_*`` 设置字段已建表但没有真正的二次认证。
5. **客户端凭据暴露范围需要重新设计。** 常规订单里有游戏账号与密码字段；授权、脱敏、查看时限、变更记录和完单后清理都没有明确实现。
6. **开放 CORS 与内存限流不适合生产。** `cors()` 允许任意来源；注册限流存于单进程 `Map`，不清理旧日期键，重启/多实例即可绕过，登录和上传也没有限流。

### P2：可维护性、质量与体验

1. **单文件过大、职责混杂。** 后端约 2,200 行、前端脚本约 3,500 行，数据库定义、支付、权限、业务和页面事件都耦合，难以测试与安全审查。
2. **数据库迁移不可靠。** 启动时执行建表并静默吞掉 `ALTER TABLE` 异常，缺少版本号、可回滚迁移和约束/索引设计审查。
3. **无自动化测试、接口契约、日志规范或健康检查。** 当前无法验证关键的下单、支付、并发、权限、库存和回调流程。
4. **数据与配置重复。** 前端保留了硬编码箱子/价格配置，后端也初始化箱子配置；未来修改容易前后不一致。
5. **样式有重复规则。** `#adminCustomList` 样式块重复；总体 CSS 和脚本继续堆叠会提高改动风险。
6. **缺少运维交付物。** 没有 README、环境变量样例、SQL 迁移、Docker/进程守护配置、备份恢复说明和发布流程。

## 6. 建议的继续开发方向

建议先确保站点安全可运行，再扩大产品功能。优先顺序如下。

### 阶段 A：恢复可运行性与止血（必须先做）

1. 规范化目录和运行方式：补 `start` 脚本，修正静态目录，提供 `.env.example`，将支付配置和密钥路径完全环境化。
2. 明确支付宝策略：安装并锁定 SDK 版本；开发环境可禁用支付模块或使用模拟支付；生产环境使用安全密钥管理、真实网关和可验证回调。
3. 移除源码中的密码/默认密钥，轮换已暴露的凭据；只允许必要的 CORS 来源。
4. 将订单、租号和积分价格计算迁移到后端，建立服务端商品/价格表、严格 DTO 校验和正数/上限校验。
5. 停止明文持久化游戏密码；若业务确需代操作账号，应采用短期加密凭据、最小权限查看、访问审计和到期清除，并先确认合规与平台规则。
6. 修复所有 `innerHTML` 数据通道：默认用 `textContent`，富文本使用成熟白名单净化器；上传改为 MIME/文件签名/大小校验和受控存储。

### 阶段 B：把业务变成可靠闭环

1. 定义正式订单状态机、状态迁移权限、取消/退款/争议/超时策略，所有资金和积分变化写入不可变流水。
2. 支付回调用数据库事务和条件更新保证幂等；校验金额、应用 ID、卖家和订单归属，增加关闭订单、对账和告警。
3. 完善租号：可用时段、并发占用、租约开始/结束、交付凭据、押金/退款、评价和违规处理。
4. 补齐管理员工作台：租号审核、内容预览、支付审核记录、订单搜索/分页、用户禁用与审计日志。
5. 实现或删除 2FA、语言设置、余额/信誉等现有“数据字段已存在但功能未闭环”的能力。

### 阶段 C：重构与工程化

1. 后端按 `routes / controllers / services / repositories / middleware / config` 分层；用统一校验库和统一错误响应。
2. 前端拆分为按领域加载的模块；视团队偏好逐步迁移到 Vite + Vue/React，或保持原生 JS 但模块化和组件化。
3. 将 schema 迁移到 Knex/Prisma/Umzug 等版本化 migration 工具，补齐外键、索引、唯一约束和数据备份方案。
4. 建立单元、接口、端到端和安全测试；至少覆盖权限边界、金额计算、并发扣减、支付回调幂等、上传和 XSS。
5. 增加 Docker、环境分层、健康检查、结构化日志、监控、错误告警和 CI/CD。

### 可选的产品扩展

- 打手认证、排班、抢单规则、SLA、评价/申诉与信用体系。
- 订单即时通知（WebSocket/短信/邮件/企业 IM）与客服工单。
- 商品兑换发货流程、开箱概率公示与完整开奖记录。
- 数据看板：GMV、订单漏斗、履约时长、打手产能、积分负债和风控指标。

## 验证记录与边界

- 已运行 `node --check backend/server.js`：语法检查通过。
- 已运行依赖清单检查：确认当前依赖树没有 `alipay-sdk`。
- 已运行一次短暂启动诊断：在导入 `alipay-sdk` 时失败，因此服务未启动、未连接 MySQL、未创建/变更数据库表。
- 本次没有修改任何现有源码、依赖、配置、数据库或静态资源；仅创建本说明文件。

在确认开发范围前，建议不要直接上线或录入真实客户账号、支付密钥和生产数据库凭据。
